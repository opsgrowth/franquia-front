node_modules
dist
dist-ssr
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json

# Envs locais — NUNCA commitar segredos. O .env.example (só chaves) fica versionado.
.env
.env.local
.env.*.local
!.env.example
